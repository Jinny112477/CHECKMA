# CHECKMA

//FRONTEND command line
git checkout frontend-dev #Check if in right branch to PUSH or PULL

git add frontend
git commit -m "..."
git push

git pull #After checkout use this command line

//BACKEND command line

//run frontend
npm install(First time)
npm bulid (First time)
npm run preview
git